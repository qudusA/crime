# DataSource configuration
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://dpg-cof99kf79t8c73cf0cq0-a/postgre_rwf4
spring.datasource.username=postgre_rwf4_user
spring.datasource.password=dpg-cof99kf79t8c73cf0cq0-a/postgre_rwf4

# Hibernate properties
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# Set the Hibernate dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Jackson settings
spring.jackson.serialization.fail-on-empty-beans=false

# Mail settings
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${mail_username}
spring.mail.password=${mail_password}
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.auth=true
app.sendfrom=${app_sendfrom}

# Twilio settings
twilio.sid=${twilio_sid}
twilio.token=${twilio_token}
twilio.phone-number=${twilio_phone-number}

# AWS settings
aws.secret-access-key=${aws_secret-access-key}
aws.access-key-id=${aws_access-key-id}
aws.region=${aws_region}
aws.bucket-name=${aws_bucket-name}

# Multipart settings
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
